// import { useContext } from "react";

// import { TasksContext } from "./TaskContext";

// export const useTasks = () => {
//   const tasks = useContext(TasksContext);

//   if (!tasks) {
//     throw new Error("Task context should be used inside of it's provider");
//   }

//   return tasks;
// };
